import React, { Component } from 'react'

export default class Landing extends Component {
  render() {
    return (
      <div>
       <h1>Welcome to a website...</h1>
       <p>we're still not quite sure what it does, but take a look around and you'll see.</p>
      </div>
    )
  }
}
